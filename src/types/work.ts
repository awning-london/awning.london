export interface Work {
    id: string
    title: string
    desc: string
    overview: string
    // benefit?: { [key: string]: string };
    // featured?: { [key: string]: string };
    btnTitle: string
    imgurl: string
  }